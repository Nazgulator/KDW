@model IEnumerable<KDW.Models.PeremeshenieWorks>

@{
    ViewBag.Title = "Index";
}

<h2>Главная</h2>

<p>
    @Html.ActionLink("Создать", "Create", null, new {@class = "btn btn-success btn-block"})
</p>
<table id="T" class="table table-bordered table-striped">
    <tr>
        <th>
            @Resources.Resource.Stock участка
        </th>
        <th>
            Буферная зона
        </th>
        <th>
          @Resources.Resource.Department
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StockUchastka)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BuferZona)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.t_Item.FName)
        </td>
        <td>
            @Html.ActionLink("Редактировать", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Удалить", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
<p><a href="/Home/Index" class="btn btn-success btn-block">В главное меню &raquo;</a> </p>
